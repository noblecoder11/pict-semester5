select * from employee natural join project natural join dept where pyear=2022;
select * from employee natural join project where plocation!='Pune';
select fname, designation, dlocation, join_date from employee natural join dept;
select * from employee natural join dept;
